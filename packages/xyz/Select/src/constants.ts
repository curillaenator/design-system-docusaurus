import type { ButtonProps } from '@kit-xyz/button';

export const DEFAULT_TEST_ID = 'XYZKit.Select';

export const MAX_OPEN_NODE_SYMBOLS = 28;

export const DEFAULT_OPEN_NODE_PROPS: ButtonProps = {
  appearance: 'primary-solid',
  size: 'xs',
  labelsFullwidth: true,
};
