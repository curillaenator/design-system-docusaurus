import type { CheckboxSizes } from './interfaces';

export const DEFAULT_TEST_ID = 'XYZKit.Checkbox';

export const SKELETON_SHAPE_ASSOC: Record<CheckboxSizes, string> = {
  xs: '0px',
};

export const SKELETON_SIZE_ASSOC: Record<CheckboxSizes, string> = {
  xs: '24px',
};
