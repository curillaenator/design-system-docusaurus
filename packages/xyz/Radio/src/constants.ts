import type { RadioSize } from './interfaces';

export const DEFAULT_TEST_ID = 'XYZKit.Radio';

export const SKELETON_SHAPE_ASSOC: Record<RadioSize, string> = {
  xs: '0px',
};

export const SKELETON_SIZE_ASSOC: Record<RadioSize, string> = {
  xs: '24px',
};
