import type { TabsSize } from '@kit-xyz/tabs';
import type { IconSize } from '@kit-xyz/icon';

export const DEFAULT_TEST_ID = 'XYZKit.Tabs';

export const ICONS_SIZE_ASSOC: Record<TabsSize, IconSize> = {
  s: 'xl',
  xs: 'xl',
};
