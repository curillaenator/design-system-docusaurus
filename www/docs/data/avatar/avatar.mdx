---
sidebar_label: 'Avatar'
title: 'Avatar'
description: 'Графическое представление пользователя'
---

import pkg from '@kit-xyz/avatar/package.json';

import { AvatarWrapped } from './components/AvatarWrapped';
import { AvatarGroup } from './components/AvatarGroup';

import { COLORS, FORMS, SIZES } from './examples/index';

import avatarGroupCode from '!!raw-loader!./components/AvatarGroup.tsx'; // eslint-disable-line import/extensions, import/no-webpack-loader-syntax

<headermeta
  title='Avatar'
  description='Графическое представление пользователя'
  copywriter='@astakhov'
  copywriterLink='https://mm.pcbltools.ru/slack-export/messages/@astakhov'
  designer='@eustinov'
  designerLink='https://mm.pcbltools.ru/slack-export/messages/@eustinov'
  developer='@kaarturov'
  developerLink='https://mm.pcbltools.ru/slack-export/messages/@kaarturov'
  versions={[pkg.version]}
  actualVersion={pkg.version}
  status='Work in progress'
  info='Information'
  bitbucketLink='https://bitbucket.pcbltools.ru/bitbucket/projects/XYZ/repos/kit/browse/packages/xyz/Badge'
  figmaLink='https://figma.com'
  jiraLink='https://jira.pcbltools.ru/jira/browse/XYZ-404'
/>

# Avatar

<versionselector versionPages={[]}>
<crutch />

## Theme

<textcolumns>
  <span>
    <p>Выявлены 5 базовых цветовых подложек, где будет использоваться элемент дизайн-системы.</p>
  </span>
</textcolumns>

<docviewport codeBlock={undefined} modes={['light', 'dark', 'color']}>
  <examples>
    {COLORS.map((card, i) => (
      <example key={card.title + 1} exampleCodeJSX={undefined}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Size

<textcolumns>
  <span>
    <p>Размеры</p>
  </span>

</textcolumns>

<docviewport modes={['light', 'dark', 'color']}>
  <examples>
    {SIZES.map(({ title, code, ...rest }) => (
      <example key={title} exampleCodeJSX={code}>
        <card title={title}>
          <AvatarWrapped {...rest} />
        </card>
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Form

<textcolumns>
  <span>
    <p>Avatar</p>
  </span>

</textcolumns>

<docviewport modes={['light', 'dark', 'color']}>
  <examples>
    {FORMS.map(({ title, code, ...rest }) => (
      <example key={title} exampleCodeJSX={code}>
        <card title={title}>
          <AvatarWrapped {...rest} />
        </card>
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Group

<textcolumns>
  <span>
    <p>Group</p>
  </span>

</textcolumns>

<docviewport modes={['light', 'dark', 'color']}>
  <examples>
    <example exampleCodeJSX={avatarGroupCode}>
      <card title='Avatars group'>
        <AvatarGroup />
      </card>
    </example>
  </examples>
</docviewport>

</versionselector>
