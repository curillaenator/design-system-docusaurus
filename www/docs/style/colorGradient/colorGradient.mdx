---
sidebar_label: 'Color gradient'
sidebar_position: 5
title: 'Color gradient'
description: 'Совмещение базовых цветов в стандартный набор градиентных переходов'
---

import headerImage from './assets/colorGradient.png';

import { GROUPS, CODEBLOCKS } from './index';

<headermeta
  title='Color gradient'
  description='Совмещение базовых цветов в стандартный набор градиентных переходов'
  copywriter='@astakhov'
  copywriterLink='https://mm.pcbltools.ru/slack-export/messages/@astakhov'
  designer='@eustinov'
  designerLink='https://mm.pcbltools.ru/slack-export/messages/@eustinov'
  developer='@kaarturov'
  developerLink='https://mm.pcbltools.ru/slack-export/messages/@kaarturov'
  versions={['1.0']}
  actualVersion='1.0'
  status='Work in progress'
  info='Information'
  bitbucketLink='https://bitbucket.pcbltools.ru/bitbucket/projects/XYZ/repos/kit/browse/packages/theme/src/scss/style/_color-gradient.scss'
  figmaLink='https://www.figma.com/'
  jiraLink='https://jira.pcbltools.ru/jira/browse/XYZ-368'
  headerImage={headerImage}
/>

# Color gradient

<versionselector versionPages={[]}>
  <crutch />

## Neutral

<docviewport codeBlock={CODEBLOCKS.neutral} language='json'>
  <examples>
    {GROUPS['neutral-700-400'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
  <div style={{ height: 96 }} />
  <examples>
    {GROUPS['neutral-700-100'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
  <div style={{ height: 96 }} />
  <examples>
    {GROUPS['neutral-400-100'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Accent-1

<docviewport codeBlock={CODEBLOCKS['accent-1']} language='json'>
  <examples>
    {GROUPS['accent-1-700-400'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
  <div style={{ height: 96 }} />
  <examples>
    {GROUPS['accent-1-700-100'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
  <div style={{ height: 96 }} />
  <examples>
    {GROUPS['accent-1-400-100'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Accent-2

<docviewport codeBlock={CODEBLOCKS['accent-2']} language='json'>
  <examples>
    {GROUPS['accent-2-700-400'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
  <div style={{ height: 96 }} />
  <examples>
    {GROUPS['accent-2-700-100'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
  <div style={{ height: 96 }} />
  <examples>
    {GROUPS['accent-2-400-100'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Informative

<docviewport codeBlock={CODEBLOCKS.informative} language='json'>
  <examples>
    {GROUPS['informative-700-400'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
  <div style={{ height: 96 }} />
  <examples>
    {GROUPS['informative-700-100'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
  <div style={{ height: 96 }} />
  <examples>
    {GROUPS['informative-400-100'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Positive

<docviewport codeBlock={CODEBLOCKS.positive} language='json'>
  <examples>
    {GROUPS['positive-700-400'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
  <div style={{ height: 96 }} />
  <examples>
    {GROUPS['positive-700-100'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
  <div style={{ height: 96 }} />
  <examples>
    {GROUPS['positive-400-100'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Attention

<docviewport codeBlock={CODEBLOCKS.attention} language='json'>
  <examples>
    {GROUPS['attention-700-400'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
  <div style={{ height: 96 }} />
  <examples>
    {GROUPS['attention-700-100'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
  <div style={{ height: 96 }} />
  <examples>
    {GROUPS['attention-400-100'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Negative

<docviewport codeBlock={CODEBLOCKS.negative} language='json'>
  <examples>
    {GROUPS['negative-700-400'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
  <div style={{ height: 96 }} />
  <examples>
    {GROUPS['negative-700-100'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
  <div style={{ height: 96 }} />
  <examples>
    {GROUPS['negative-400-100'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

</versionselector>
