---
sidebar_label: 'Color transparent'
sidebar_position: 4
title: 'Color transparent'
description: 'Содержит общую информацию о цветах и правилах системного шага прозрачности цветов'
---

import headerImage from './assets/colorTransparent.png';

import { TRANSPARENCIES, CODEBLOCKS } from './index.tsx';

<headermeta
  title='Color transparent'
  description='Содержит общую информацию о цветах и правилах системного шага прозрачности цветов'
  copywriter='@astakhov'
  copywriterLink='https://mm.pcbltools.ru/slack-export/messages/@astakhov'
  designer='@eustinov'
  designerLink='https://mm.pcbltools.ru/slack-export/messages/@eustinov'
  developer='@kaarturov'
  developerLink='https://mm.pcbltools.ru/slack-export/messages/@kaarturov'
  versions={['1.0']}
  actualVersion='1.0'
  status='Work in progress'
  info='Information'
  bitbucketLink='https://bitbucket.pcbltools.ru/bitbucket/projects/XYZ/repos/kit/browse/packages/theme/src/scss/style/__color-transparent.scss'
  figmaLink='https://www.figma.com/'
  jiraLink='https://jira.pcbltools.ru/jira/browse/XYZ-107'
  headerImage={headerImage}
/>

# Color transparent

<versionselector versionPages={[]}>
<crutch />

## Neutral-700

<docviewport codeBlock={CODEBLOCKS['neutral-700']} language='json'>
  <examples>
    {TRANSPARENCIES['neutral-700'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Neutral-400

<docviewport codeBlock={CODEBLOCKS['neutral-400']} language='json'>
  <examples>
    {TRANSPARENCIES['neutral-400'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Neutral-100

<docviewport codeBlock={CODEBLOCKS['neutral-100']} language='json' modes={[]}>
  <examples>
    {TRANSPARENCIES['neutral-100'].map((card, i) => (
      <example key={card.id} colorMode='dark' exampleCodeJSON={card.code}>
        <card {...card} colorMode='dark' />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Accent-1-700

<docviewport codeBlock={CODEBLOCKS['accent-1-700']} language='json'>
  <examples>
    {TRANSPARENCIES['accent-1-700'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Accent-1-400

<docviewport codeBlock={CODEBLOCKS['accent-1-400']} language='json'>
  <examples>
    {TRANSPARENCIES['accent-1-400'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Accent-1-100

<docviewport codeBlock={CODEBLOCKS['accent-1-100']} language='json' modes={[]}>
  <examples>
    {TRANSPARENCIES['accent-1-100'].map((card, i) => (
      <example key={card.id} colorMode='dark' exampleCodeJSON={card.code}>
        <card {...card} colorMode='dark' />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Accent-2-700

<docviewport codeBlock={CODEBLOCKS['accent-2-700']} language='json'>
  <examples>
    {TRANSPARENCIES['accent-2-700'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Accent-2-400

<docviewport codeBlock={CODEBLOCKS['accent-2-400']} language='json'>
  <examples>
    {TRANSPARENCIES['accent-2-400'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Accent-2-100

<docviewport codeBlock={CODEBLOCKS['accent-2-100']} language='json' modes={[]}>
  <examples>
    {TRANSPARENCIES['accent-2-100'].map((card, i) => (
      <example key={card.id} colorMode='dark' exampleCodeJSON={card.code}>
        <card {...card} colorMode='dark' />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Informative-700

<docviewport codeBlock={CODEBLOCKS['informative-700']} language='json'>
  <examples>
    {TRANSPARENCIES['informative-700'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Informative-400

<docviewport codeBlock={CODEBLOCKS['informative-400']} language='json'>
  <examples>
    {TRANSPARENCIES['informative-400'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Informative-100

<docviewport codeBlock={CODEBLOCKS['informative-100']} language='json' modes={[]}>
  <examples>
    {TRANSPARENCIES['informative-100'].map((card, i) => (
      <example key={card.id} colorMode='dark' exampleCodeJSON={card.code}>
        <card {...card} colorMode='dark' />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Positive-700

<docviewport codeBlock={CODEBLOCKS['positive-700']} language='json'>
  <examples>
    {TRANSPARENCIES['positive-700'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Positive-400

<docviewport codeBlock={CODEBLOCKS['positive-400']} language='json'>
  <examples>
    {TRANSPARENCIES['positive-400'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Positive-100

<docviewport codeBlock={CODEBLOCKS['positive-100']} language='json' modes={[]}>
  <examples>
    {TRANSPARENCIES['positive-100'].map((card, i) => (
      <example key={card.id} colorMode='dark' exampleCodeJSON={card.code}>
        <card {...card} colorMode='dark' />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Attention-700

<docviewport codeBlock={CODEBLOCKS['attention-700']} language='json'>
  <examples>
    {TRANSPARENCIES['attention-700'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Attention-400

<docviewport codeBlock={CODEBLOCKS['attention-400']} language='json'>
  <examples>
    {TRANSPARENCIES['attention-400'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Attention-100

<docviewport codeBlock={CODEBLOCKS['attention-100']} language='json' modes={[]}>
  <examples>
    {TRANSPARENCIES['attention-100'].map((card, i) => (
      <example key={card.id} colorMode='dark' exampleCodeJSON={card.code}>
        <card {...card} colorMode='dark' />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Negative-700

<docviewport codeBlock={CODEBLOCKS['negative-700']} language='json'>
  <examples>
    {TRANSPARENCIES['negative-700'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Negative-400

<docviewport codeBlock={CODEBLOCKS['negative-400']} language='json'>
  <examples>
    {TRANSPARENCIES['negative-400'].map((card, i) => (
      <example key={card.id} exampleCodeJSON={card.code}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Negative-100

<docviewport codeBlock={CODEBLOCKS['negative-100']} language='json' modes={[]}>
  <examples>
    {TRANSPARENCIES['negative-100'].map((card, i) => (
      <example key={card.id} colorMode='dark' exampleCodeJSON={card.code}>
        <card {...card} colorMode='dark' />
      </example>
    ))}
  </examples>
</docviewport>

</versionselector>
