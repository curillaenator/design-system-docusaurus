---
sidebar_label: 'Switch'
title: 'Switch'
description: 'Компонент для создания группы переключателей'
---

import pkg from '@kit-xyz/switch/package.json';

import { COLORS, STATES } from './examples/index';
import { SwitchWrapped } from './examples/SwitchWrapped';

<headermeta
  title='Switch'
  description='Компонент для создания группы переключателей'
  copywriter='@astakhov'
  copywriterLink='https://mm.pcbltools.ru/slack-export/messages/@astakhov'
  designer='@eustinov'
  designerLink='https://mm.pcbltools.ru/slack-export/messages/@eustinov'
  developer='@kaarturov'
  developerLink='https://mm.pcbltools.ru/slack-export/messages/@kaarturov'
  versions={[pkg.version]}
  actualVersion={pkg.version}
  status='Work in progress'
  info='Information'
  bitbucketLink='https://bitbucket.pcbltools.ru/bitbucket/projects/XYZ/repos/kit/browse/packages/xyz/Switch'
  figmaLink='https://figma.com'
  jiraLink='https://jira.pcbltools.ru/jira/browse/XYZ-152'
/>

# Switch

<versionselector versionPages={[]}>
<crutch />

## Theme

<textcolumns>
  <span>
    <p>Выявлены 5 базовых цветовых подложек, где будет использоваться элемент дизайн-системы.</p>
  </span>
</textcolumns>

<docviewport codeBlock={undefined} modes={['light', 'dark', 'color']}>
  <examples>
    {COLORS.map((card, i) => (
      <example key={card.title + 1} exampleCodeJSX={undefined}>
        <card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## State

<textcolumns>
  <span>
    <p>Состояния поведения</p>
  </span>

</textcolumns>

<docviewport modes={['light', 'dark', 'color']}>
  <examples>
    {STATES.map(({ title, code, ...rest }) => (
      <example key={title} exampleCodeJSX={code}>
        <card title={title}>
          <SwitchWrapped {...rest} />
        </card>
      </example>
    ))}
  </examples>
</docviewport>

<divider />

</versionselector>
